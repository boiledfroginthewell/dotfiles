#!/bin/bash

selected=$(git status -s | \
	fzf -m --ansi --cycle --layout=reverse \
	--preview 'git diff -- $(echo {} | cut -c 4-)')

if [ -n "$selected" ];then
	git add -- $(echo "$selected" | cut -c 4-) &
fi

