% dpkg, debian, ubuntu
# show installed packages
apt list --installed

% kmonad
# show systemd's user unit log
journalctl --user-unit kmonad

% systemd
# restart systemd's user unit
systemctl restart --user kmonad.service

% arch
# install without any interaction
yay --noconfirm

